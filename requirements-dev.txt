# Local development / full on-prem dependencies (NOT installed on Vercel).
# Includes the production runtime plus: local dev server, tests, MCP server,
# optional Postgres driver, and the heavy optional ML packages (XGBoost,
# LightGBM) that would blow the serverless function bundle budget.
-r requirements.txt
uvicorn>=0.34,<1
pytest>=8,<10
httpx>=0.28,<1
psycopg[binary]>=3.2,<4
xgboost>=3,<4
lightgbm>=4,<5
mcp>=1.12,<2
